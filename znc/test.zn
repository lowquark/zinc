
struct MyStruct {
  int a;
  int b;
  Child child;

  access my_module;
}

struct MyStruct:Child {
  float c;
}

module my_module {
  int var_a;
  int var_b;

  function bla(int a, int b, int c) {
    return a + b + c;
  }

  function func1() {
    int local_a;
    int local_b;
    int local_c;

    local_a = (11*1)+(0*1);
    local_b = 2;
    local_c = -9;

    bla(local_a, local_b, local_c);

    if(local_c == -9) {
    } else if(local_b == 13) {
      int local_d;
      local_c = 1000;
      local_d = 3;
    } else {
      local_c = 1000000;
    }

    return (local_b - local_a == local_c)*57;
  }
}

